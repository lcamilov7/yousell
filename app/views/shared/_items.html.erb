<%= link_to 'Add product', new_product_path, data: { turbo_frame: 'modal' }, class: 'text-sm mx-2 text-orange-500 font-bold hover:text-orange-700' if Current.user.present? %>
<%= link_to 'Categories', categories_path, class: 'text-sm mx-2 text-orange-500 font-bold mx-2 font-bold hover:text-orange-700' if Current.user&.admin? %>
<%= link_to 'Favorites', favorites_path, class: 'text-sm mx-2 text-orange-500 font-bold mx-2 font-bold hover:text-orange-700' if Current.user.present? %>
<%= link_to 'My Products', user_path(username: Current.user.username), class: 'text-sm mx-2 text-orange-500 font-bold mx-2 font-bold hover:text-orange-700' if Current.user&.admin? %>
<%# <%= link_to 'Log In', new_session_path, class: 'mx-2 text-orange-500 font-bold mx-2 text-orange-500 font-bold hover:text-orange-700' unless Current.user.present? %>
<%= link_to 'Access', new_user_path, class: 'text-sm mx-2 text-orange-500 font-bold mx-2 font-bold hover:text-orange-700' unless Current.user.present? %>
<%= link_to 'Log Out', session_path(Current.user.id), data: { turbo_method: :delete, turbo_confirm: 'Log out?' }, class: 'text-sm mx-2 text-orange-500 font-bold mx-2 font-bold hover:text-orange-700' if Current.user.present? %>
