<div id='<%= dom_id(product) %>' class='shadow relative rounded bg-neutral-600 p-5 w-2/5 me-2 flex flex-col justify-center items-center'> <%# este dom_id nos servira para turbosstream action cable %>
  <h1 class='title font-bold text-2xl text-orange-500'><%= product.title %></h1>
  <span class='price font-bold text-gray-200'>$<%= product.price %></span>
  <div class='text-gray-200 min-h-1/3'>
    <p class='description font-light text-sm'><%= product.description %></p>
  </div>

  <%= image_tag(product.photo, width: 200, class: 'rounded shadow') if product.photo.attached? %>
  <%= render partial: 'favorite', locals: { product: @product } %> <%# aca hay que pasar la variable como local para poder hacer el turbo stream lo podemos ver en favorites controller %>
</div>
